package io.github.dantegrek.jplay;

class JsStrings {
    static final String JS_PSEUDO_ELEMENT_CONTENT = "el => getComputedStyle(el, '%s').content";
    static final String JS_PSEUDO_ELEMENT_PROPERTY = "el => getComputedStyle(el, '%s').getPropertyValue('%s')";

    private JsStrings() {
    }
}
